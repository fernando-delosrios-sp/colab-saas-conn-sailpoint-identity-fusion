🧪 [testing improvement description]
🎯 **What:** Added tests for `extractAccountInfoFromFormInput` to ensure proper data extraction from form inputs. Also, fixed a bug where the function failed when `formInput` was `null` or `undefined`.
📊 **Coverage:** Covered all structures the function can handle (flat, object with specific properties, and dictionaries). Fallback cases and edge cases like missing or `null` inputs are properly covered.
✨ **Result:** Improved test coverage and fixed a crash scenario in `formProcessor.ts`.
