## 2026-08-22 - Dry-run sendEmail JSON structure clarity
**Learning:** The dry-run `sendEmail` parameter supports a single string or an array of strings, but this was only mentioned textually. Readers misinterpret parameter structure unless concrete JSON payloads are provided as examples.
**Action:** Add concrete JSON code blocks for API payloads, utilizing array format for `sendEmail` to show its usage clearly.
