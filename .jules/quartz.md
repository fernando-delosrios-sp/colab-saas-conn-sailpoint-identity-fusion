## 2024-08-20 - Remove redundant trim utility
**Learning:** Creating local aliases for global utilities (like `trimOrUndefined` wrapping `trimStr`) increases mental overhead for future readers by forcing them to look up what the local alias does.
**Action:** Use the global utility directly.
