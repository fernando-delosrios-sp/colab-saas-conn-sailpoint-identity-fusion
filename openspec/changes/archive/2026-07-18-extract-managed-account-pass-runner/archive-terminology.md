# Archive terminology

This change was archived before **reconcile-matching-delegation-spec** (2026-08-09). Artifacts here may use period-accurate names such as `ManagedAccountPassRunner`, `ManagedAccountMatchingRunner`, or `setCaptureBreakdown`.

**Current canonical terms:** `MatchOutcomeDispatcher` (sweep orchestration + outcome dispatch), `MatchingService.configureScoring({ captureBreakdown })` (scoring prep).

See [archive README](../README.md) for the full historical mapping. Normative behavior is defined in `openspec/specs/` living specs.
